<Window x:Class="DiscordGameOverlay.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Discord Game Overlay - Setup"
        Width="450"
        Height="360"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">

    <Grid Margin="25">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Discord Game Overlay Setup"
                   FontSize="22"
                   FontWeight="Bold"
                   Margin="0,0,0,20"/>

        <TextBlock Grid.Row="1"
                   Text="Discord Bot Token"
                   Margin="0,0,0,5"/>

        <PasswordBox x:Name="TokenBox"
                     Grid.Row="2"
                     Height="32"
                     Margin="0,0,0,15"/>

        <StackPanel Grid.Row="3">

            <TextBlock Text="Discord Channel ID"
                       Margin="0,0,0,5"/>

            <TextBox x:Name="ChannelIdBox"
                     Height="32"/>

        </StackPanel>

        <TextBlock Grid.Row="4"
                   Text="This configuration is stored only on this computer."
                   Foreground="Gray"
                   TextWrapping="Wrap"
                   Margin="0,15,0,15"/>

        <Button Grid.Row="5"
                Content="Save Settings"
                Width="120"
                Height="35"
                HorizontalAlignment="Right"
                Click="SaveButton_Click"/>

    </Grid>
</Window>