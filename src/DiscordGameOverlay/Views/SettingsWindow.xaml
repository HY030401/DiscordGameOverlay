<Window x:Class="DiscordGameOverlay.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Discord Game Overlay - Setup"
        Icon="/DiscordGameOverlay;component/Assets/AppIcon.ico"
        Width="450"
        Height="625"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">

    <Grid Margin="25">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Discord Game Overlay Setup"
                   FontSize="22"
                   FontWeight="Bold"
                   Margin="0,0,0,20"/>

        <TextBlock Grid.Row="1"
                   Text="Discord Bot Token"
                   Margin="0,0,0,5"/>

        <PasswordBox x:Name="TokenBox"
                     Grid.Row="2"
                     Height="32"
                     Margin="0,0,0,15"/>

        <StackPanel Grid.Row="3">

            <TextBlock Text="Discord Channel ID"
                       Margin="0,0,0,5"/>

            <TextBox x:Name="ChannelIdBox"
                     Height="32"/>

            <TextBlock Text="扔屎自定义表情 ID"
                       Margin="0,12,0,5"/>

            <TextBox x:Name="PoopEmojiIdBox"
                     Height="32"/>

            <TextBlock Text="鸽子屎自定义表情 ID"
                       Margin="0,12,0,5"/>

            <TextBox x:Name="PigeonPoopEmojiIdBox"
                     Height="32"/>

            <TextBlock Text="爱心中箭自定义表情 ID"
                       Margin="0,12,0,5"/>

            <TextBox x:Name="HeartEmojiIdBox"
                     Height="32"/>

            <TextBlock Text="扔鸡蛋自定义表情 ID"
                       Margin="0,12,0,5"/>

            <TextBox x:Name="EggEmojiIdBox"
                     Height="32"/>

        </StackPanel>

        <TextBlock Grid.Row="4"
                   Text="表情 ID 可留空以禁用对应特效；配置只保存在本机。"
                   Foreground="Gray"
                   TextWrapping="Wrap"
                   Margin="0,15,0,15"/>

        <Button Grid.Row="5"
                Content="Save Settings"
                Width="120"
                Height="35"
                HorizontalAlignment="Right"
                Click="SaveButton_Click"/>

    </Grid>
</Window>
